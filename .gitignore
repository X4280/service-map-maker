node_modules/
.vercel
.env
.env*.local
.DS_Store

# Never commit — this repo is public on GitHub. The Red Bull image is served
# only through api/redbull.js (password-gated, backed by private Redis).
assets/redbull.gif
local_preview_punchy_redbull.html
